import { DisplayImageFilterPipe } from './display-image-filter.pipe';

describe('DisplayImageFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayImageFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
